class Element:
    def __init__(,tid,iutils,rutils):
    """
    pass tid,iutils,rutils as a number

    """

	.tid = tid
	.iutils = iutils
	.rutils = rutils

	print_depth(depth):
		for count,value in enumerate(depth):
            print("\t");
		    print("\t"+tid+" iutils: "+iutils+" rutils: "+rutils);
